# Example environment variables for local development.
# Copy this file to `.env` and adjust values as needed. Do NOT commit your real `.env`.

# Default test subset used by the pre-commit pytest wrapper. Adjust to a faster subset
# of tests you want to run at commit time, or remove to run full suite during commits.
PRE_COMMIT_TESTS=apps/core/tests
########################################
# Local / Example environment variables
########################################
# Use these names or the prefixed variants expected by `django_settings_env`.
# The project uses `django_settings_env.Env` which prefixes names with `DJANGO_`
# by default. For example `DJANGO_SECRET_KEY` maps to the `SECRET_KEY` setting.

# Core
DJANGO_DEBUG=True
DJANGO_SECRET_KEY=replace-with-generated-secret-key
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# HTTPS / cookies / HSTS (production examples)
# In development these can remain False/0. In production set appropriately.
DJANGO_SECURE_SSL_REDIRECT=False
DJANGO_SESSION_COOKIE_SECURE=False
DJANGO_CSRF_COOKIE_SECURE=False
DJANGO_SECURE_HSTS_SECONDS=0
DJANGO_SECURE_HSTS_INCLUDE_SUBDOMAINS=False
DJANGO_SECURE_HSTS_PRELOAD=False
DJANGO_USE_PROXY_SSL_HEADER=False

# Email (example)
DJANGO_DEFAULT_FROM_EMAIL=noreply@example.com
DJANGO_EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DJANGO_EMAIL_HOST=localhost
DJANGO_EMAIL_PORT=587
DJANGO_EMAIL_USE_TLS=True
DJANGO_EMAIL_HOST_USER=
DJANGO_EMAIL_HOST_PASSWORD=

# Social Login Credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

FACEBOOK_CLIENT_ID=your_facebook_client_id
FACEBOOK_CLIENT_SECRET=your_facebook_client_secret

INSTAGRAM_CLIENT_ID=your_instagram_client_id
INSTAGRAM_CLIENT_SECRET=your_instagram_client_secret

# Stripe Credentials
STRIPE_PUBLISHABLE_KEY=pk_test_placeholder
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_placeholder

# NowPayments
NOWPAYMENTS_API_KEY=your_nowpayments_api_key
